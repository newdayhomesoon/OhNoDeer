Place the rasterized foreground layers for each density (ic_launcher_foreground.png) into mipmap-mdpi, -hdpi, -xhdpi, -xxhdpi, -xxxhdpi. Current placeholder created only for hdpi.
Suggested export background color: #0A1929 (matches app theme). The provided deer logo should be centered with padding ~16% on each side to avoid clipping in adaptive masks.
